<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="joindao">
	
	<insert id="generalJoin" parameterType="memberdto">
		<selectKey resultType="int" keyProperty="memberNo" order="BEFORE">
			select count(memberNo) from member
		</selectKey>
		
		insert into member values(#{memberNo} + 1, #{memberId}, #{memberPwd}, #{memberName}, #{memberPhone}, #{addrPost}, #{addrMain}, #{addrDetail}, #{memberEmail}, #{memberBirth}, #{memberGender}, '1', null)
	</insert>
	
</mapper>


